global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'stg-validator-metrics'
    static_configs:
      - targets: ['localhost:6060'] # Port metrik internal blockchain daemon
